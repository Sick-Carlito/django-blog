# .env.example -- copy this to ".env" and replace placeholders with real values.
# DO NOT commit your real .env to source control.

# Django secret key (generate with Django util and paste here)
SECRET_KEY=z%ka8^wjo61n41al&xfnaf(@g102mjkyn9+bw=gb42wq)6$bp(

# Debug mode: True for development, False for production
DEBUG=True

# Allowed hosts: comma separated list (e.g., "example.com,www.example.com")
ALLOWED_HOSTS=127.0.0.1,localhost

# DATABASE_URL example (SQLite for dev)
DATABASE_URL=sqlite:///db.sqlite3

# If you prefer to use DB_URL instead of DATABASE_URL in your environment,
# add DB_URL here too (prod systems commonly use DATABASE_URL).
DB_URL=

# For production you will override with:
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# Optional: production flags you can set
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=3600
